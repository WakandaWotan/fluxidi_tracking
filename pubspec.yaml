name: fluxidi_tracking
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # === Fluxidi tracking dependencies ===
  geolocator: ^12.0.0
  http: ^1.2.2
  permission_handler: ^11.3.1

  # === Mapbox (official) ===
  mapbox_maps_flutter: ^2.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/fluxidi/

